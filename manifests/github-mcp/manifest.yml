applications:
- name: github-kl-mcp-server
  instances: 1
  memory: 124M
  buildpacks:
    - binary_buildpack
  env:
    GITHUB_TOOLSETS: context,orgs,repos,pull_requests
  command: "./github-mcp-server --gh-host https://github.com stdio"
  routes:
    - route: github-kl-mcp-server.apps.internal